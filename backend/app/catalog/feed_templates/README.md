# Repository feed-template extension point

Versioned templates for this iteration are synthetic fixtures under `backend/tests/fixtures/catalog_feed_mappings/`. This directory is reserved for future checked-in, reviewed templates; no runtime registry or external connector is used.
